const altura_cm = 172;
const altura_metro = 1.72;
const peso_kg = 68.5;
const altura_red = Math.ceil(altura_metro);
const peso_red = Math.floor(peso_kg);
const sonIguales = Number.MAX_VALUE + 1 === Number.MAX_VALUE;
