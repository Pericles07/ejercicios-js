
const nombre = "Pedro";
const apellido = "Lozano";
const estudiante = nombre + " " + apellido;
const estudianteMayus = estudiante.toUpperCase();
const estudianteMinus = estudiante.toLowerCase();
const estudianteLength = estudiante.length
const letraInicial = nombre[0];
const letraFinal = apellido[apellido.length -1];
const sinEspacios = estudiante.replace(/ /g,"");
const nombreIncluido = estudiante.includes(nombre);

